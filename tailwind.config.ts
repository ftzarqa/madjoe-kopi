// ============================================================
// tailwind.config.ts
// CATATAN: Proyek ini menggunakan Tailwind CSS v4.
// Di v4, konfigurasi dilakukan via CSS (@theme di globals.css),
// bukan via file ini. File ini sengaja dikosongkan.
//
// Untuk konfigurasi warna, font, animasi → lihat:
//   src/app/globals.css  (bagian @theme)
// ============================================================

import type { Config } from "tailwindcss";

const config: Config = {
  content: [],
};

export default config;
